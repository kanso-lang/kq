# Retired instructions, counted by callgrind on the linux runner. The vein the
# allocator counters cannot reach: both quadratics behind the 147 ms row
# allocated nothing new, so every other gate in this repo stayed green across a
# 3.7x change in the published number.
#
# Which runner, made checkable. bench/instructions.sh reads the line below and
# refuses to measure anywhere else, because the sentence above it is a rule
# that nothing enforced: kanso pasted a container's numbers over its runner's
# on 2026-08-23 on the strength of a diff, and two ubuntu 24.04 boxes one glibc
# revision apart differ by more than most of what this vein catches.
# measured-on glibc=2.39-0ubuntu8.8
#
# The glibc revision is only half the host. Its ifunc resolvers pick memcpy,
# memcmp and strlen by CPU feature at load time, so the same libc on other
# silicon counts differently — 0.63% of spread from the dispatch alone, and
# 1.02% on print_small for a single switch that differs between a container and
# a runner. That half CANNOT be pinned on this line, and the reason is
# measured: three CI runs on 2026-09-01 landed on three different CPUs — an
# AMD EPYC Zen 3 (family 0x19, model 0x1), an Intel Ice Lake-SP (0x6/0x6a),
# against the Cascade Lake (0x6/0x55) of the container the first block was
# prepared in. A line refusing every run but one would be red for a reason no
# pull request causes.
#
# So bench/instructions.sh prints the CPU on every run and consults it only
# when a row has actually moved: a row that landed on its recorded value is
# right whatever counted it, and a row that moved is asked whether this is even
# the same silicon before it is called a regression.
#
# 2026-09-01, pin b32c5c77 -> 6727b5a2 (fifteen kanso merges). All four rows
# rise, and THE COMPILER IS NOT WHY.
#
#     print_small   76,695,994 ->  76,742,853    +46,859  +0.061%
#     print_big    777,577,892 -> 778,046,419   +468,527  +0.060%
#     path_small    20,010,298 ->  20,029,238    +18,940  +0.095%
#     path_big     207,264,436 -> 207,452,126   +187,690  +0.091%
#
# Three of the fifteen merges touch src/ or lib/ at all: kanso#1188 (the inline
# name), #1197 (the at-risk memo) and #1200 (the survivorship counter). kq was
# built and counted against each of the four compilers in a container, one path
# and one toolchain throughout, and the four arms are the same to within
# fourteen instructions on every row:
#
#     pin         print_small   print_big    path_small   path_big
#     b32c5c77    76,742,444    778,046,228  20,028,811   207,451,713
#     #1188       76,742,430    778,046,242  20,028,825   207,451,713
#     #1197       76,742,430    778,046,242  20,028,825   207,451,713
#     #1200       76,742,430    778,046,242  20,028,825   207,451,713
#
# So fifteen merges are worth fourteen instructions and the runner reports
# forty-six thousand. The container reads 409 below the runner, which is the
# same offset kq#83 recorded on 2026-08-31 — and 76,742,444 + 409 is 76,742,853,
# the runner's number under the NEW pin. The runner under the OLD pin should
# have read that too, and the row committed for it says 76,695,994.
#
# ANSWERED, an hour later, and the first guess was wrong. It was not a
# toolchain. Both job logs print every version and they agree to the commit
# hash: rustc 1.98.0 (88d9e12ae), LLVM 22.1.8, runner image ubuntu-24.04
# 20260823.283.1, glibc 2.39-0ubuntu8.8, valgrind 3.22.0-0ubuntu3, gdb 15.1.
# Nothing that compiles or runs this binary moved.
#
# One line differs. kq#83 ran in Azure region centralus and this one in eastus,
# which is a different physical CPU — and glibc selects memcpy, memcmp, strlen
# and their neighbours through ifunc, by CPU feature, at load time. Same libc,
# different implementation, different instruction count for the same bytes
# moved. That is why pinning the glibc REVISION does not pin this vein, and why
# the shift is small (0.06% to 0.10%) and in the same direction on all four
# rows.
#
# The inference is the ifunc dispatch; what is measured is that every printed
# version matches and the region does not. Since written, the loader's own
# feature block has been read in the job and confirms it, and the fix landed in
# the shape described at the top of this file rather than as a measured-on
# fact — because the pool turned out to hold at least three CPUs, and a fact
# that names one of them refuses the other two.
#
# Watched, not frozen. A rise is a regression to explain and a fall is a win to
# bank; moving the pin moves these rows and that is the point of having them.
# Say which way and why in the PR, then regenerate.
#
# The big fixture is ten flat copies of bench/large.json, so a row that is
# linear in the input lands near 10x its small sibling. Both do, which is the
# same thing bench/scale_gate.kso asserts about the allocator counters and the
# first time it has been asserted about work:
#
#   print  793,574,242 / 78,436,396 = 10.12x
#   path   218,906,819 / 21,300,482 = 10.28x
#
# First sitting 2026-08-14, against the compiler in .kanso-version — the pin
# carrying kanso #895 and #888, the two fixes that took the print row from
# 147 ms to 39.8 ms.
# 2026-08-20: every row falls under the pin bump c7f302c6 -> cc26898a —
# print_small -331,611 (-0.4%), print_big -2,256,286 (-0.3%), path_small
# -304,464 (-1.4%), path_big -2,243,856 (-1.0%). Forty kanso commits moved
# codegen (the alternating fixpoint sweep among them); the surrogate
# machinery this PR adds runs only on `\u` escapes, which these inputs do
# not contain. Banked, not explained row by row.
# 2026-08-20 (shorthands): print rows split — print_small falls 340 (two
# fewer frozen startup constants) while print_big pays 12,078 for the two
# new jump-table arms its escape-heavy output compares against; both path
# rows fall by ~1,300. Net well under a thousandth of a percent per row.
# 2026-08-20 (the literal is the number): both path rows fall ~4.1% —
# decode no longer parses a single float, so Eisel-Lemire's work leaves
# the path entirely — while print_big pays 1.44M (+0.18%) for the GDA
# reassembly of exponent-marked literals and print_small nets slightly
# down. The trade the change makes: decode cheaper, print marginally
# dearer, and every byte identical to jq.
# 2026-08-23: every row falls under the pin bump cc26898a -> 2601a7a5, and the
# shape says where it comes from. The small rows each lose 6,250 and the big
# rows each lose 62,500 — ten times over, matching the ten copies the big
# fixture is made of — and print and path lose the same amount as each other.
# A cost that scales with the input and is identical across the two operations
# lives in the reading and decoding they share.
#
# That rules out the change the window is named for. Eleven kanso commits are
# in it, the std/os split among them, and the split saves module-init work done
# before any input is read. This file has twice established that such a saving
# lands as a flat offset across fixtures of very different sizes, and a flat
# offset cannot present as a 10x spread. Banked without a row-by-row account,
# with the shape recorded so the next reader starts at the input path.
# 2026-08-15: all four rows rise, 0.12% to 0.35%. The pin walks twenty
# commits forward and what it collects is kanso's escaped-buffer registry
# (kanso#912), which every program links whether or not it grows an
# accumulator past its beat. kanso's own vein recorded the same band on its
# six benchmarks. kq's allocator counters move the other way over the same
# window — evacuations 989 to 533, evacuated bytes 488,624 to 395,376 — and
# this vein cannot see that, which is the division of labour between the two.
# 2026-08-17: all four rows rise by EXACTLY 74 instructions, and the constant
# is the finding. print_big does forty times the work of path_small and pays
# the same 74, so this is a fixed cost per run rather than a cost per row —
# 0.00009% on the big print and 0.0003% on the small path.
#
# The pin walks eleven commits to kanso cd1142e7, carrying gavel 20 (a
# guarded self-reference completes at its constructor) and 20b's cycle guard.
# What kq collects is the completion itself: k_caf_complete now answers the
# seed cell with the frozen value, which is a couple of stores per constant,
# paid once at k_caf_init before any input is read. The render guard it also
# carries is a test per FORCED CELL rendered, and kq renders none — which is
# exactly why these rows show a flat offset instead of one that grows with
# the output.
#
# kanso's own vein recorded +74 on encodebench and widebench over the same
# window, the same number for the same reason.
# 2026-08-17 (later): all four rows FALL by exactly 18, and the constant is
# again the finding. print_big does forty times the work of path_small and
# collects the same 18, so this is a fixed cost per run rather than per row.
#
# The pin carries gavel 20b's second half: rendering forces what it prints
# and a container goes on the render path. The prediction written into the
# PR was that kq would RISE, because it renders on every row it prints and
# the container path is paid per rendered container. That prediction is
# wrong and the direction is the evidence — a per-row cost cannot show up as
# a flat offset across fixtures forty times apart.
#
# What the rows actually collect: k_render's thunk arm lost its early-out
# and now forces, but kq renders no cell that has not run, so that branch is
# never taken and nothing per-row changes. Eighteen instructions of layout
# around code the linker places differently, in kq's favour this time.
# kanso's own vein recorded -18 on encodebench, oneshot and widebench over
# the same window, the same number for the same reason.
#
# 2026-08-26: all four rows RISE, and unlike every entry above this one the
# rise is proportional to the DOCUMENT rather than flat.
#
#     print_small  78,370,357 ->  78,758,842   +388,485   +0.50%
#     path_small   20,172,881 ->  20,560,668   +387,787   +1.92%
#     print_big   793,645,399 -> 797,583,853 +3,938,454   +0.50%
#     path_big    208,407,698 -> 212,343,588 +3,935,890   +1.89%
#
# print and path collect the SAME number on the same fixture — 388k on the
# small one, 3.94M on the big — so the cost is in the decode both share and
# not in printing or in path extraction. It is ten times larger on the ten
# times larger document, which is what a per-element cost looks like.
#
# Most of it is kanso gavel 24: an arm cannot see an err its own hako raised,
# and that is dispatch semantics, so an arm that could match its own hako's
# failure asks before it matches, every time it runs. Measured rather than
# inferred, by building this repo against kanso main with the ruling and
# against main without it, on one host so the glibc offset cancels:
#
#     print_small  78,409,395 -> 78,758,433   +349,038
#     path_small   20,211,413 -> 20,560,255   +348,842
#
# So gavel 24 is about 349k of the 388k, and the remaining 39k is the rest of
# the twenty merges this pin bump absorbs. The allocator counters are the
# other half of the story and they FELL — the guard allocates nothing, which
# is exactly why this vein exists and why cost_golden could not see it.
#
# kanso's own vein recorded the same shape on the same day: jsonbench +1.81%,
# oneshot +0.74%, and three benchmarks whose hot paths hold no err-admitting
# arm did not move at all.
#
# 2026-08-29, absorbing seventy-three merges (kanso #1043 -> #1120): print
# rows rise 0.42% and path rows fall 0.07%, and one commit is nearly all of
# both. kanso#1052, "Two failures in a record merge, and `some` is not a
# failure", bisected on a container where the two SMALL rows read:
#
#   #1051 (the commit before)   print_small 78,758,433   path_small 20,560,255
#   #1052                       print_small 79,089,693   path_small 20,545,520
#   #1120 (this pin)            print_small 79,090,274   path_small 20,545,552
#
# So #1052 is +331,260 and -14,735, and the sixty-eight merges after it add
# +581 and +32 between them. That container cannot compare its absolute
# numbers with the runner's — a glibc revision apart, about 410 instructions
# on these rows — but its DELTAS matched the runner's to the instruction,
# which is what made the bisect trustworthy and cheap.
#
# The trade reads the way the change reads. Printing builds records, so it
# pays the merge that makes a construction holding two failures answer both;
# `.[0].k0_30` selects one field and builds none, so it does not. Every
# allocator counter in this repo is byte-identical across the same window,
# which is the second time this vein has seen something they could not.
#
# 2026-08-31, absorbing sixty-one merges (kanso #1121 -> #1181): every row
# falls, print by about 1.4% and path by 1.7 to 1.9%. The rows were measured
# at #1180; #1181 edits only markdown.
#
#     print_small  79,090,683 ->  77,933,867  -1,156,816  -1.46%
#     path_small   20,545,965 ->  20,153,719    -392,246  -1.91%
#     print_big   800,902,056 -> 789,934,094 -10,967,962  -1.37%
#     path_big    212,175,336 -> 208,611,129  -3,564,207  -1.68%
#
# Print and path do not collect the same number here, so the saving does not
# live in the decode they share. Bisected on a container at seven points in
# the window, small rows only:
#
#   #1143  a subtype of a primitive is a heap value  print +128,464  path   +186
#   #1171  two byte-level costs in the runtime       print -955,739  path -320,184
#   #1172-#1173  the string index gets the cursor    print +113,192  path   +470
#   #1174-#1177  the wide benchmark's two costs      print  -40,141  path  -59,157
#   #1178-#1180  the tenure walk answers on ranges   print -402,592  path  -13,561
#
# Both columns sum to the end-to-end delta exactly.
#
# Fifty of the sixty-one commits move neither row by one instruction: two
# runs of them, 22 commits from #1121 to #1142 and 26 from #1145 to #1170,
# plus #1144 and #1181, which edit only markdown. The rest of that fifty is
# front-end work, and this vein counts what the compiled program executes, so
# a change that only makes compiling cheaper cannot show up here. kanso's own
# compile veins recorded every one of them.
#
# The two large movers split the way their subjects predict. #1171 is byte
# handling in the runtime, which decoding and rendering both do, and it is the
# only entry that takes a large amount off both rows. #1178 and #1179 rewrote
# the tenure membership test, and they take 402,592 off print against 13,561
# off path: that test is asked by the mutation fast paths, and printing
# appends to a buffer where selecting one field does not.
#
# #1143 is the one rise, and it splits the same way: 128,464 on print against
# 186 on path, a cost paid per value built rather than per value read.
#
# The container that produced these deltas is one glibc revision behind the
# runner. Under the pinned compiler it read print_small 79,090,274 against
# this file's 79,090,683, a 409-instruction offset matching the one the
# 2026-08-29 entry recorded. So the rows below are the runner's committed
# values moved by the container's deltas, and CI is what confirms them.
#
# 2026-08-31, the pin moves five kanso merges to b32c5c77. Every row falls
# again, and this time one commit is the whole of it.
#
#     print_small   77,933,867 -> 76,695,994   -1,237,873   -1.588%
#     print_big    789,934,094 -> 777,577,892  -12,356,202   -1.564%
#     path_small    20,153,719 ->  20,010,298     -143,421   -0.712%
#     path_big     208,611,129 -> 207,264,436   -1,346,693   -0.646%
#
# Four of the five merges cannot reach this vein: #1182 fixes a staging
# collision in a rust test, #1183 and #1184 move the log and the compiler
# page, and #1185 writes an attribution and a branch list. None of them
# touches src/ or lib/.
#
# #1186 is the one, and it is two changes to the byte builder's append.
# k_b_append_into wrote its grow path twice, differing in the allocator and
# the sign of a cap and nothing else, and one tail serves both now. Then the
# growth went out of line: the fast path needs two registers and the growth
# needs eight, so every append was pushing six callee-saved registers and
# building a frame before it could test anything, and it pushes three now.
# kq prints by appending to a buffer, which is why print falls twice as far
# as path.
#
# Every allocation counter here is byte-identical across the bump — all three
# cost goldens pass unregenerated, the scale gate passes, and numbers_gate
# says the published numbers still describe the compiler. This is the case
# the file's header exists for: kanso's own veins recorded a 3.06% fall on
# its encode benchmark and not one allocation counter moved, here or there.
#
# The container that produced these deltas is one glibc revision behind the
# runner, and read the pinned compiler's rows 409, 177, 413 and 413 below
# this file's. So the rows below are the committed values moved by the
# container's deltas. kanso#1186 had four rows where that arithmetic came out
# fourteen instructions high, so CI confirming them is the point rather than
# a formality.
#
# 2026-09-01: pin past kanso#1206. Four merges, two of them substantial runtime
# work, and every row falls: print_small -6.81%, print_big -6.64%, path_small
# -2.18%, path_big -1.68% measured in one container against the old pin; the
# rows below are the runner's, counted on the recorded AMD Genoa, so the
# dispatch question the vein asks on a moved row answered "same silicon".
#
# kanso#1205 straight-lined the one-byte byte-builder append and marked `k_die`
# noreturn, so a runtime entry that validates a tag stopped building a frame for
# an error path it never takes. kanso#1206 stopped `k_ten_release` recomputing a
# sixty-four-deep summary on every beat pop. Both are instructions the common
# path paid for a rare one, which is why the print rows — where kq spends most
# of its work — move four times what the path rows do.
#
# Not one allocation counter moved. cost_golden.txt, cost_golden_decode.txt and
# cost_golden_escapes.txt each gained the two rows kanso#1206 added to the
# counter dump (`ten_blocks`, `ten_frees`, both nought here) and are otherwise
# byte-identical, which is the whole reason this vein exists.
# 2026-09-01 (later): pin past kanso#1208, and every row falls again.
#
#   print_small   71,520,430 ->  71,208,591    -311,839  -0.436%
#   print_big    726,349,766 -> 723,231,394  -3,118,372  -0.429%
#   path_small    19,593,071 ->  19,281,689    -311,382  -1.589%
#   path_big     203,961,167 -> 200,847,635  -3,113,532  -1.527%
#
# kanso#1208 replaced `k_utf8_bad`'s byte-at-a-time tail with an overlapping
# load ladder: eight or more bytes read whole words and then re-read the LAST
# eight, and a shorter run does the same a step down with four and two. The
# mean run the function is handed is seven bytes, which is why the walk was
# doing nearly all the work. On kanso's own corpus jsonbench fell 1.238%.
#
# The shape of the fall says the same thing from here. print_small and
# path_small give up 311,839 and 311,382 instructions; print_big and path_big
# give up 3,118,372 and 3,113,532. A saving that tracks the document decoded
# and barely notices what is done with it afterwards is a saving in the decode,
# which is where the ascii test lives. The path rows fall by four times the
# percentage because they do so much less work per byte read.
#
# THESE ROWS WERE NOT COUNTED ON THE RECORDED SILICON, and this is the entry
# where that stopped mattering. The gate says to re-run until the job lands on
# the recorded AMD Genoa; two runs landed on two DIFFERENT other chips, family
# 0x6 model 0xcf and family 0x6 model 0xad, whose glibc dispatch blocks differ
# from the Genoa's and from each other in memcpy thresholds, cache geometry and
# the `Fast_Unaligned_*` preferences. All four rows came out BYTE-IDENTICAL on
# both. So for kq's four programs the pool's silicon does not move this vein at
# all, and chasing a third chip would have bought nothing.
#
# That is not a licence to stop recording which chip counted a row. It is one
# measurement, on four programs, and the reason the vein asks the question is
# that a row CAN move with dispatch. bench/dispatch.txt still records the
# Genoa, so a future move still gets asked. kanso#1208's own entry found the
# same thing from the other side: its five deltas were identical on a Cascade
# Lake and a Genoa.
#
# No allocation counter moved. The change touches no allocator.
print_small 71208591
print_big 723231394
path_small 19281689
path_big 200847635
