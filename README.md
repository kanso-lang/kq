# kq

A `jq`-style JSON query tool, written in [kanso](https://kanso-lang.dev). Output
is byte-identical to `jq -S` on every query below — verified with `diff`, not
claimed.

## Speed

Interleaved runs (kq and jq alternate, so machine state hits both alike),
whole-process wall time (startup + read + parse + query + print), fastest of
fifteen to twenty-five repetitions per row. Byte-identity verified before any
timing. Apple M4 Max, **2026-08-14**, `bench/kq_race.sh`. The 1.9 MB fixture is
ten flat copies of `bench/large.json`'s elements, so it is reproducible from
what the repo already carries.

<!-- raced:begin — a dated sitting, moved by hand. CI regenerates the
     deterministic counters in bench/MEASURED.md and leaves these rows
     alone; see bench/publish_numbers -->

kq against jq 1.7.1. The `work` column is retired instructions, which no other
process on the box can move — where a clock disagrees with it, the clock is
reporting the machine and this column is reporting the program. Two sittings an
hour apart agreed on every wall figure to within 1.3% and on every instruction
ratio exactly.

These rows used to be regenerated by CI on every pull request. Two consecutive
runs of identical code moved every one of them by nine to twenty-two per cent
and reversed the direction of the largest, because a shared runner is a
different physical machine each time and alternating the two programs cannot
cancel that. Interleaving handles the noise within a run, which is not where
the error was.

| workload | kq | jq 1.7.1 | | work |
|---|---:|---:|---|---|
| path query, 188 KB | 2.6 ms | 4.5 ms | kq 1.7x faster | 1.98x less |
| path query, 1.9 MB | 11.6 ms | 24.4 ms | kq 2.1x faster | 1.92x less |
| full pretty-print, 188 KB | 5.0 ms | 12.2 ms | kq 2.4x faster | 2.94x less |
| full pretty-print, 1.9 MB | 39.8 ms | 103.4 ms | kq 2.6x faster | 3.07x less |

<!-- raced:end -->

The last row read 147 ms on 2026-08-09, the one workload jq won. It was
quadratic twice over, and both costs sat in the same place: the loop that
streams one element at a time carries the whole remaining document across each
iteration, and the runtime asked two questions about that carried list per
element rather than once. The survival check walked it, and the evacuation
deep-copied its items buffer. On a sixteen-thousand element array the pair cost
11.6 billion retired instructions where 130 million was enough. kanso #895
memoises the first and #888 tenures a value that has already survived a lap, so
the second stops copying it.

Before that it read 1113 ms, ten times jq's, from a third quadratic: kanso #639
changed when the copy walk may share a node. The counter behind it read 2,721
on the 188 KB fixture and 286,401 on ten times the data, and now reads 69 and
717. `bench/scale_gate.kso` is the gate that refuses its return, and
`bench/scale_exceptions.txt` is empty again.

None of the three showed up in the deterministic counters below. A check that
allocates nothing and a copy that lands in an existing buffer both leave those
figures alone, which is why the wide-array shape now has a row of its own in
kanso's `bench/instructions_golden.txt`.

<!-- measured:begin — regenerated by bench/publish_numbers.kso on every push;
     edit that, not this -->

Deterministic allocator counters, regenerated by CI on every commit. Every
figure reproduces on any machine, and `ratio` is ten times the document over
the 188 KB fixture — work that is linear in the input lands near 10.

| counter | 188 KB | 2.1 MB | ratio |
|---|---:|---:|---|
| `allocs` | 89607 | 895675 | 9.9x |
| `alloc_bytes` | 5478736 | 54707364 | 9.9x |
| `arena_peak_bytes` | 3145728 | 22858512 | 7.2x |
| `carry_dedup` | 69 | 717 | 10.3x |
| `view_allocs` | 2761 | 27610 | 10.0x |
| `view_frees` | 0 | 0 | — |

<!-- measured:end -->

| full pretty-print, 1.9 MB | kq | jq 1.7.1 |
|---|---:|---:|
| wall clock | **39.8 ms** | 103.4 ms |
| retired instructions | **3.07x fewer** | — |
| cycles | **2.73x fewer** | — |
| instructions per cycle | 4.65 | 5.23 |
| peak footprint | **28.0 MB** | 30.7 MB |

Measured in one interleaved sitting on 2026-08-14, alternating the two binaries
so neither gets a warm machine the other did not. jq issues more instructions
per cycle and still finishes later, because kq gives it three times less work
to do.

The machinery under those rows is kanso's, and it is worth a paragraph. The
arena rewinds between loop iterations when the compiler proves the iteration
keeps nothing across the line, and a byte accumulator earns that proof by
pointer identity: it is the very object that arrived at the loop's entry,
threaded through appends the uniqueness analysis showed in place, with its
growth outside the arena where a rewind cannot reach. Every loop on kq's
print path qualifies, so each element's temporaries die the moment the next
element begins.

What remains of kq's footprint is the decoded document — live across the
whole run by construction — plus the decode-phase transients the region
holds until exit. The output no longer contributes: it streams.

Absolutes here carry the load; a quiet box brings every row down.

The path-query gap grows with document size: kq decodes, walks to the subtree,
and prints only that — the win compounds as the part you didn't ask for gets
bigger. Pretty-printing is jq's board at scale again while the copy-walk
regression stands: the byte builder in the encode path (one accumulator
threaded through the whole tree, escape scanning proven clean in one SIMD
pass) still wins the smaller documents and loses the large ones to the walk.

**One deliberate difference:** on a path that doesn't exist, `jq` prints
`null`; kq reports an error naming the missing key. kanso treats a missing
index as a failure to surface, not a nothing to pass along — if you want
jq's silence, query a path that exists. The race harness verifies
byte-identity per query before timing anything, which is exactly how this
difference was caught.

## Use

```
kq <path> [file.json]        # or pipe json on stdin
kq .users[3].name data.json
```

## Why it's fast

kq is ~400 lines of kanso sharing its decoder with the standard library — the
same decoder that outruns hand-tuned serde_json on the language's json
gauntlet. No hand-written parser tricks live in this directory; the speed is
the compiler's. The story: [kanso-lang.dev/compiler.html](https://kanso-lang.dev/compiler.html).

## Specs

`sh spec.sh` (with `KANSO` pointing at a kanso build) runs the unit tests,
then eleven fixture cases over non-trivial JSON — unicode/CJK/emoji and
escapes, precision-edge numbers, deep nesting and empty containers, and the
188 KB nested document — each checked against a committed golden AND against
live `jq -S` byte-for-byte. CI gates on all of it.

Intel macs: no GitHub runners exist for that target anymore; build from
source (`kanso build .`) or use Rosetta until a cross-build lands.
